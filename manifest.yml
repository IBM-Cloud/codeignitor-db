---
declared-services:
  mysql-db:
    label: mysql
    plan: 100
  twilio:
      label: ci-twilio
      plan: 'user-provided'
applications:
- name: codeignitor-twilio-demo
  memory: 128M
  instances: 1
  host: codeignitor-twilio-demo
  path: .
  buildpack: https://github.com/cloudfoundry/php-buildpack.git
  services:
  - mysql-db
